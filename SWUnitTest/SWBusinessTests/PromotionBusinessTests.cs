using SWBusiness;
using SWDomain.DataTransferObjects;
using SWDomain.Entities;
using System;
using Xunit;

namespace SWUnitTest.SWBusinessTests
{
    public class PromotionBusinessTests
    {
        //[Theory, ClassData(typeof(DTOCartItem))]
        //[InlineData(new DTOCartItem() { Item = new Item() { Id = 0, Name = "", Price = 4.0m, Promotion = 1 }, Count = 2 }, 4.0m)]
        //public void TestApplyPromotion(DTOCartItem cartItem, decimal expected)
        //{
        //    decimal result = new PromotionBusiness().ApplyPromotion(cartItem);

        //    Assert.Equals(result, expected);
        //}
    }
}
